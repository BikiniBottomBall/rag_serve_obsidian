# DeepSeek API Key (required for RAG query generation)
DEEPSEEK_API_KEY=sk-your-api-key-here

# Path to Obsidian vault (absolute, or relative to working directory)
VAULT_PATH=opencode知识库

# Where to store the vector index (absolute, or relative to working directory)
STORAGE_DIR=storage
